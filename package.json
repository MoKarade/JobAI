{
  "name": "jobai",
  "version": "0.1.0",
  "private": true,
  "description": "Suivi et analyse de recherche d'emploi (région de Québec) — app du hub perso.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@mokarade/hub-contract": "github:MoKarade/hub-contract#v1.0.0",
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2",
    "next": "^15.5.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.23.8"
  },
  "//overrides": "Failles HIGH dans des dépendances transitives que Next épingle lui-même : postcss (XSS + lecture de fichier arbitraire via sourceMappingURL) et sharp. À retirer dès qu'une version de Next les remonte d'elle-même — vérifier avec `npm audit --omit=dev`.",
  "overrides": {
    "postcss": "^8.5.24",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.16",
    "@eslint/eslintrc": "^3.3.6",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9.18.0",
    "eslint-config-next": "^15.5.4",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
